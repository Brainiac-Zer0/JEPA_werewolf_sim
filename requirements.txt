# AgentSim — pinned to the environment these fixes were validated against
# (Python 3.14, CPU). For GPU training install the matching CUDA torch build
# instead of the CPU wheel.

torch==2.13.0            # CPU wheel here (…+cpu); use the CUDA build for GPU runs
numpy==2.5.0
pandas==3.0.5
scipy==1.18.0
scikit-learn==1.9.0
pyyaml==6.0.3

# Belief/message encoder + local (offline) speaker/judge models
transformers==5.14.1
sentence-transformers==5.6.1

# Optional UI (headless runs do not need a display)
pygame-ce==2.5.7

# Required ONLY for the OpenAI speaker/judge backend (LLM_PROVIDER=openai).
# Not needed for CPU/offline runs (LLM_PROVIDER=hf) or when language is disabled.
# openai>=1.0
